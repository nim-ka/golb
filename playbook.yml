- name: Golb setup
  hosts: all
  tasks:
    - name: Update APT sources
      become: yes
      apt:
        update_cache: yes

    - import_role:
        name: share

    - import_role:
        name: samba

    - import_role:
        name: nginx

    - import_role:
        name: motd
